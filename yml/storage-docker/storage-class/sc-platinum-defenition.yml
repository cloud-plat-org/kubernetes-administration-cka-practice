---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: platinum
# spec: No spec: in StorageClass definition
  provisioner: kubernetes.io/gce-pd  # provisioner to use
  parameters:
    type: pd-ssd
    replication-type: regional-pd
